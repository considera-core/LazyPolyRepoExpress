# Auto detect text files and perform LF normalization
* text=auto

# cmd.exe fails to resolve GOTO/CALL labels in batch files with LF-only line
# endings ("The system cannot find the batch label specified"), so these must
# always land in the working tree as CRLF regardless of core.autocrlf.
*.bat text eol=crlf
*.cmd text eol=crlf
