{
  "name": "leprechaun-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-module-stories.mjs",
    "dev": "npm run generate && storybook dev -p 6006",
    "storybook": "npm run dev",
    "build": "npm run generate && storybook build"
  },
  "devDependencies": {
    "@storybook/addon-docs": "^10.6.0",
    "@storybook/html-vite": "^10.6.0",
    "storybook": "^10.6.0",
    "vite": "^7.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
