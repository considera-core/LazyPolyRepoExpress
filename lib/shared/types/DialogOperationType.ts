/**
 * Which operation a modify dialog was opened to perform. Drives the dialog's title verb and whether the form
 * starts pristine (`edit`) or already dirty (`add`).
 */
export type DialogOperationType = 'view' | 'add' | 'edit';
